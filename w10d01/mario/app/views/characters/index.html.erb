<h1>All the characters for <%= @game.name %></h1>

<%= link_to "All the games", games_path %>

<!-- <% @characters.each do |character| %>
  <%= render 'character', character: character %>
<% end %> -->

<%= render @characters %>
